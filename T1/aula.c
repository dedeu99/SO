#include <pthread.h>
#include <unistd.h>

static void* child(){
	printf("Sou uma thread\n");
}

int main(){
	int core=pthread.create(id,atributos, argumentos da função, child);
	pthread.join
	

	return 0;
}
